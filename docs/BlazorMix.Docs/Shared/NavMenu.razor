
@foreach (var componentName in AppRoutes.Routes)
{
    if (!string.IsNullOrEmpty(componentName))
    {
        <NavItem Href="@("/components/" + componentName)">
            @(componentName)
        </NavItem>
    }
}