@namespace BlazorMix.Components
@inherits BmComponentBase

@if (If == null || If.Invoke())
{
    @ChildContent
}

@code {

    [Parameter] public RenderFragment ChildContent { get; set; }

    [Parameter] public Func<bool> If { get; set; }
}